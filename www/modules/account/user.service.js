(function () {
    'use strict';
    angular.module('starter')
        .factory('user', function () {
            var user = {
                data: {}
            };

            return user;
        });
})();